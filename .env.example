# Client (Vite) build — no env vars are required.
# The missed-call demo posts to the same-origin /api/demo route; the browser
# never holds any Supabase or n8n credentials.
#
# Server-side secrets for /api/demo live in Cloudflare (see .dev.vars.example
# for local dev, and the Cloudflare Pages dashboard for production).
