# Supabase — used client-side by the missed-call demo form (src/scripts/demo-request.js)
# to insert into the `demo_requests` table.
#
# These are PUBLIC, publishable values, safe to ship in the browser bundle:
#   - the URL is public
#   - the anon/publishable key is write-restricted by Row Level Security
#     (policy `anon_insert_demo_requests` only permits INSERT into demo_requests)
#
# Do NOT put the Supabase service_role key or any Twilio credentials here or
# anywhere in this frontend — SMS sending happens server-side (n8n + Twilio).
#
# Copy this file to `.env.local` (gitignored) to enable the demo locally.

VITE_SUPABASE_URL=https://sibyvtphxadrqnwamgdt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_lNehfjnCQL1axjfOJSIIKA_UewdFP2_
